query events {
  findAllEvents {
    id
    title
    eventImageURL
    eventDetails
  }
}


query event($where: EventWhereUniqueInput!) {
  findOneEvent(where: $where) {
    id
    title
    eventImageURL
    eventDetails
  }
}