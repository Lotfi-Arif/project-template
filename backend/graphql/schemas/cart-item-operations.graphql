# Add or adjust the input argument types to match your GraphQL schema.

# Query to get a cart item by ID
query GetCartItem($id: String!) {
  cartItem(id: $id) {
    id
    productId
    quantity
    # Add other fields of the CartItem model as needed
  }
}

# Mutation to add a new cart item
mutation AddCartItem($data: CartItemCreateInput!) {
  addCartItem(data: $data) {
    id
    productId
    quantity
    # Add other fields of the CartItem model as needed
  }
}

# Mutation to update a cart item
mutation UpdateCartItem($data: CartItemUpdateInput!, $where: CartItemWhereUniqueInput!) {
  updateCartItem(data: $data, where: $where) {
    id
    productId
    quantity
    # Add other fields of the CartItem model as needed
  }
}

# Mutation to remove a cart item
mutation RemoveCartItem($id: String!) {
  removeCartItem(id: $id) {
    id
    # Add other fields of the CartItem model as needed
  }
}
