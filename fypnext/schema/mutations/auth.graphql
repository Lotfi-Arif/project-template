mutation loginUser($data: LoginInput!) {
  loginUser(data: $data) {
    accessToken
    refreshToken
  }
}

mutation signup($data: SignupInput!) {
  signup(data: $data) {
    accessToken
    refreshToken
  }
}
