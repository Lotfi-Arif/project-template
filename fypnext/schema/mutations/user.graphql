mutation signup($data: SignupInput!) {
  signup(data: $data) {
    accessToken
    refreshToken
  }
}

mutation rejectUser($id: String!) {
  rejectUser(id: $id) {
    id
  }
}

mutation approveUser($id: String!) {
  approveUser(id: $id) {
    id
  }
}