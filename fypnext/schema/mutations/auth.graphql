mutation login($data: LoginInput!) {
  login(data: $data) {
    accessToken
    refreshToken
  }
}

mutation signup($data: SignupInput!) {
  signup(data: $data) {
    accessToken
    refreshToken
  }
}
