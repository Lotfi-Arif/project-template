query events {
  findAllEvents {
    id
    title
    eventImageURL
    eventDetails
  }
}
query event($id: String!) {
  findOneEvent(id: $id) {
    id
    title
    eventImageURL
    eventDetails
  }
}