mutation createChat($data: ChatCreateInput!) {
  createChat(data: $data) {
    id
    chatName
    Message {
        id
        Message
    }
    users {
      id
      firstName
      lastName
    }
  }
}
